# GEDCOM_Parser_Project - runtime requirements
#
# Keep this file lean: only packages imported by the actual pipeline/CLI/runtime.
# (Dev-only tools like black/pytest belong in requirements-dev.txt.)

PyYAML>=6.0          # config/gedcom_parser.yml support
rich>=13.7           # rich logging used by postprocess modules
typer>=0.12          # CLI entrypoints in src/gedcom_parser/cli
jsonschema>=4.19     # schema validation in verify_all_*.sh scripts
